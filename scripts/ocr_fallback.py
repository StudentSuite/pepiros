# TODO (NEW): PaddleOCR-VL (Baidu, 0.9B doc VLM) local script. Fires when parse.py's zero-extractable-text check hits,
# or a page is table/formula-dense. Outputs structured Markdown/JSON/OTSL tables/LaTeX, merged back into
# chunks/figures/numerics by lib/services/ingest.ts. Local script only, same pattern as seed.ts -- no deployed service.
