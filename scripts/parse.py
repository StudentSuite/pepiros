# TODO: PyMuPDF parse. page.search_for(quote) for anchoring (primary, fast, gives rect lists directly). Section-bounded
# chunking, 500-800 tokens, 80 overlap, never crosses a section boundary. Two-column reading-order reconstruction here only.
