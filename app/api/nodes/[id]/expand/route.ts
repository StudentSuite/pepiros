// TODO: POST. Generate children for a leaf's followup chip. Calls lib/agents/orchestrator.ts for one generator.
import { notImplemented } from "@/lib/api/notImplemented";

export async function POST() {
  return notImplemented("POST /api/nodes/[id]/expand");
}
