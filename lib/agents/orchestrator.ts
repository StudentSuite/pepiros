// TODO: p-queue concurrency 4, prompt-cached on the paper block, per-node failure isolation (one failed generator = retry
// button on that leaf, never breaks the graph). Fans out to the 21 generators. See PLAN-V1.md §8.
