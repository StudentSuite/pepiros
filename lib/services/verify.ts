// TODO: the deterministic verifier. fuzzy quote match (token_set_ratio >= .92 located, >=.75 paraphrase) + numeric
// entailment floor against the numerics ledger. No LLM judge. Server-side re-verify on every MCP create_node call —
// never trust a client-asserted quote_located. See PLAN-V1.md §4.4.
