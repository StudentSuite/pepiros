// TODO: page.search_for(quote) via PyMuPDF (called from scripts/parse.py, results stored on chunks). Fallback: sliding
// window over get_text("words") for ligatures/soft hyphens. Multi-span anchors required, not optional. See PLAN-V1.md §4.3.
