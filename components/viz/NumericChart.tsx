// TODO (NEW): Recharts panel per pillar, plots effect sizes/CIs/p-values sourced from the numerics table via /api/graph
// or a dedicated numerics endpoint. Zero new backend logic, reuses lib/grounding/numeric.ts output. See plan.md flow.
