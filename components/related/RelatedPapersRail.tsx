// TODO (NEW): right rail on the default doc-reader view. Fetches /api/related, renders title/tl;dr/citationCount cards.
// Populates in <1s (no LLM in the path). First real content the user sees after the skeleton graph. See plan.md flow.
